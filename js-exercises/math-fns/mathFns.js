const sqrt = (inputValue) => Math.sqrt(inputValue)
const power = (inputValuePower, inputValueToBePowered) => Math.pow(inputValuePower, inputValueToBePowered)
const round = (inputValue) => Math.round(inputValue)

export {
  sqrt,
  power,
  round,
};
